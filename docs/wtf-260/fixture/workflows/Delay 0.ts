import { elementClicked, setCustomState } from "@buildprint/bubblescript";
export default elementClicked("Delay 0", {actions:[setCustomState({element:"wtf-260-autobinding",state:"Save delay",value:0})]});
